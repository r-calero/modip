$(".compound-filter").html("<%= escape_javascript(render(:partial => "compound_ranking", :locals => {:target => target.id, :first => first, :second => second, :third => third}))%>");
SPINNER.load('.definition');
