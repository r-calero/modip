<%= javascript_include_tag 'spinner' %>
<%= javascript_tag "$(function(){
	SPINNER.load('.definition')
});" %>
<%= stylesheet_link_tag 'users', :media => :all %>
<h1> Ranking list </h1>
<h2> Energies are sorted by difference between first and second columns </h2>
<div class="row-fluid">
  <div class="row-fluid">
    <%= form_tag(compound_rank_user_url) do %>
    <% if(target) %>
       <div class="row_fluid">
       		<%= collection_select :target, :target_id, Target.all, :id, :name, {:selected => target} %>
       </div>
       <div class ="row-fluid">
        <% receptor_types = target.receptor_types %>
        <div class="span3"><%= collection_select :filter, :filter_one, receptor_types, :id, :name, {:selected => first.to_s}, {:class => "filter"} %></div>
        <div class="span3"><%= collection_select :filter, :filter_two, receptor_types, :id, :name, {:selected => second.to_s}, {:class => "filter"} %></div>
        <div class="span3"><%= collection_select :filter, :filter_tree, receptor_types, :id, :name, {:selected => third.to_s}, {:class => "filter"} %></div>
        <div class="span3"><%= submit_tag "Filter by", :class => 'btn' %></div>
       </div>
      <% end %>
    <% end %>
  </div>
  <div class="row-fluid">
    <div class="compound-filter">
      <% if(target) %>
          <%= render(:partial => 'compound_ranking', :locals => {:target => target.id, :first => first, :second => second, :third => third}) %>
      <% else %>
          <h3 class="text-center text-warning">There isn't any target in the system. </h3>
      <% end %>
    </div>
  </div>
  
</div>
